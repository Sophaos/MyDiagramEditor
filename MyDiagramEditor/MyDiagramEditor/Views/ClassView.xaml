<UserControl x:Class="MyDiagramEditor.Views.ClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <StackPanel Margin="5">
        <TextBlock>Class name :</TextBlock>
        <TextBox x:Name="ItemName" Width="200" Height="20"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"></TextBox>
        <TextBlock>Attributs :</TextBlock>
        <TextBox x:Name="Attributs" Width="200" Height="60"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"></TextBox>
        <TextBlock>Methods :</TextBlock>
        <TextBox x:Name="Methods" Width="200" Height="60"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"></TextBox>
        <Button x:Name="Apply" Margin="0 5 0 0">Apply</Button>
        <Button x:Name="Cancel" Margin="0 5 0 0">Cancel</Button>
    </StackPanel>
</UserControl>
